$('#follow_<%= @user.id %>').html("<%= j(render "relationships/follow", user: @user) %>");
$('#followers_count').html("<%= @user.followers.count %>");
$('#followeds_count').html("<%= @user.followeds.count %>");
